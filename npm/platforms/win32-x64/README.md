# codex-rescue-win32-x64

Platform binary package for the Codex Rescue Alpha5 prerelease. CI injects the PyInstaller-built Windows x64 executable before `npm pack`. The repository does not contain a prebuilt binary.
